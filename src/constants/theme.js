import { DefaultTheme } from 'react-native-paper';

export const theme = {
    ...DefaultTheme,
    colors: {
      ...DefaultTheme.colors,
      primary: '#4077c4',
      secondary: '#FAFAFA',
      accent: '#888a',
      error: '#f13a59',
    },
};
